program fibonacci
{
    declare y;
    function fibonacci(in x)
    {
        if(x <= 1)
            return (x);;
        return (fibonacci(in x-1)+fibonacci(in x-2));
    }
    # main #
    input(y);
    print(fibonacci(in y));
}.